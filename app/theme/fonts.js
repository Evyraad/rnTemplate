// import { Platform } from 'react-native';

// const isIOS = Platform.OS === 'ios';

const type = {
  latin: {
    // regular: isIOS ? 'ios_font_name' : 'android_font_name'
  },
};

const size = {
  h1: 38,
  h2: 34,
  h3: 30,
  h4: 26,
  h5: 20,
  h6: 19,
  input: 18,
  regular: 16,
  medium: 14,
  small: 12,
  tiny: 8.5,
};

export default {
  type,
  size,
};
